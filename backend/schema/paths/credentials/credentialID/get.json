{
	"operationId": "getCredential",
	"summary": "Get a stored DNS credential",
	"tags": [
		"credentials"
	],
	"security": [
		{
			"bearerAuth": [
				"credentials.view"
			]
		}
	],
	"description": "Returns metadata for one stored DNS credential. Secret payload is not exposed.",
	"responses": {
		"200": {
			"description": "OK",
			"content": {
				"application/json": {
					"schema": {
						"$ref": "../../../components/credential-object.json"
					}
				}
			}
		}
	}
}
