{
	"operationId": "deleteCertificate",
	"summary": "Delete a Certificate",
	"tags": [
		"certificates"
	],
	"security": [
		{
			"bearerAuth": [
				"certificates.manage"
			]
		}
	],
	"description": "Deletes a certificate record. Does not remove files already deployed to disk until nginx is reloaded.",
	"parameters": [
		{
			"in": "path",
			"name": "certID",
			"description": "Certificate ID",
			"schema": {
				"type": "integer",
				"minimum": 1
			},
			"required": true,
			"example": 2
		}
	],
	"responses": {
		"200": {
			"description": "200 response",
			"content": {
				"application/json": {
					"examples": {
						"default": {
							"value": true
						}
					},
					"schema": {
						"type": "boolean"
					}
				}
			}
		}
	}
}
