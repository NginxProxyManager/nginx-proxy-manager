{% if caching_enabled == 1 or caching_enabled == true -%}
  # Asset Caching
  {% if forwarding_mode != "direct" and upstream_servers.size > 0 %}
  {% include "_assets_upstream.conf" %}
  {% else %}
  include conf.d/include/assets.conf;
  {% endif %}
{% endif %}
