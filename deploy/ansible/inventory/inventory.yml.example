---
# Legacy example — CI uses inventory/hosts.yml (committed).
# Inventory name oci_test avoids hyphen parsing in ansible --limit.
npm_test:
  hosts:
    oci_test:
      ansible_host: oci-test.eh168.alexson.org
