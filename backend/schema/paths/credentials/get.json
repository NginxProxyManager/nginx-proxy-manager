{
	"operationId": "listCredentials",
	"summary": "List stored DNS credentials",
	"tags": [
		"credentials"
	],
	"security": [
		{
			"bearerAuth": [
				"credentials.view"
			]
		}
	],
	"description": "Lists DNS credentials stored in the encrypted NPM vault on the data volume.",
	"responses": {
		"200": {
			"description": "OK",
			"content": {
				"application/json": {
					"schema": {
						"$ref": "../../components/credential-list.json"
					}
				}
			}
		}
	}
}
