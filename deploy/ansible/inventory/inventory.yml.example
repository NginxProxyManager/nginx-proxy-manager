# Copy to inventory/hosts.yml and set ansible_host to your target server.
# Use an inventory name without hyphens for --limit (e.g. npm_hosts).

all:
  children:
    npm_hosts:
      hosts:
        npm_server:
          ansible_host: nginx-proxy-manager.example.com
