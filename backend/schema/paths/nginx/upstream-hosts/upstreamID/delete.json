{
	"operationId": "deleteUpstreamHost",
	"summary": "Delete an Upstream Host",
	"tags": ["upstream-hosts"],
	"security": [{ "bearerAuth": [] }],
	"parameters": [
		{
			"in": "path",
			"name": "upstreamID",
			"schema": { "type": "integer", "minimum": 1 },
			"required": true,
			"example": 1
		}
	],
	"responses": {
		"200": {
			"description": "200 response",
			"content": {
				"application/json": {
					"schema": {
						"type": "boolean"
					}
				}
			}
		}
	}
}
