{
	"operationId": "listCredentials",
	"summary": "List stored DNS credentials",
	"tags": ["credentials"],
	"security": [{ "bearerAuth": ["credentials.view"] }],
	"responses": {
		"200": {
			"description": "OK",
			"content": {
				"application/json": {
					"schema": {
						"type": "array",
						"items": { "$ref": "../../components/credential-object.json" }
					}
				}
			}
		}
	}
}
