import { migrate as logger } from "../logger.js";

/**
 * Migrate
 *
 * @see http://knexjs.org/#Schema
 *
 * @param   {Object}  knex
 * @returns {Promise}
 */
const up = (_knex) => {
	return Promise.resolve(true);
};

/**
 * Undo Migrate
 *
 * @param   {Object}  knex
 * @returns {Promise}
 */
const down = (_knex) => {
	return Promise.resolve(true);
};

export { up, down };
