---
# Legacy example — use inventory/hosts.yml (committed) for the test host.
# Inventory name oci_test avoids hyphen parsing in ansible --limit.
npm_test:
  hosts:
    oci_test:
      ansible_host: oci-test.lab.eh168.alexson.org
