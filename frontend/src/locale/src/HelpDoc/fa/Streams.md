## استریم چیست؟

Stream در Nginx برای فوروارد کردن مستقیم ترافیک TCP/UDP به یک میزبان دیگر در شبکه استفاده می‌شود.

این قابلیت برای سرویس‌هایی مانند سرورهای بازی، FTP، SSH و سایر سرویس‌های مبتنی بر TCP یا UDP کاربرد دارد.
